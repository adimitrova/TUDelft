import java.util.ArrayList;

class Solution {
	
	/**
	 * Takes an array list and removes last occurence of x,
	 * shifting the rest of the elements left.
	 * I.e. [1, 5, 9, 8], with x = 5
	 * would result in: [1,9,8].
	 * Note that this method does not return a new list.
	 * Instead, the list that is passed as a parameter is changed.
	 * @param list to remove an element from.
	 * @param x element value to look for
	 */
	public static void removeLastOccurence(int x, ArrayList<Integer> list) {

	}
	/**
	 * Removes all elements from the array list in order.
	 * @param list to remove the elements from, should be empty after.
	 */
	public static void removeAll(ArrayList<Integer> list) {

	}
}