class Solution {
  
  /**
   * @param n
   * @returns the sum of all positive integers less then or equal to n.
   */
  public static int sumFor(int n) {
    return 0;
  }
  
  /**
   * @param n
   * @returns the sum of all positive integers less then or equal to n.
   */
  public static int sumWhile(int n) {
    return 0;
  }
  
  /**
   * @param n
   * @returns the sum of all positive integers less then or equal to n.
   */
  public static int sumRec(int n) {
    return 0;
  }
  
}//
